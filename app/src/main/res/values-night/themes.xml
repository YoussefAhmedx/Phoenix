<resources xmlns:tools="http://schemas.android.com/tools">
     Base application theme.
    <style name="Theme.Phoenix" parent="Theme.AppCompat.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimaryVariant">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="android:background">@color/black</item>
    </style>

    <!-- Splash Screen Theme -->
    <style name="WelcomeSplashScreen" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/welcome_splash_screen_night</item>
    </style>

    <!-- Widget Theme -->

    <!-- Hint Text Theme -->
    <style name="hint_text">
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:textSize">30sp</item>
    </style>
    <!-- Lateral Text -->
    <style name="lateral_text">
        <item name="android:textSize">16sp</item>
        <item name="android:textColor">@color/dark_gray</item>
        <item name="android:layout_marginStart">10dp</item>
    </style>
    <!-- Button Theme -->
    <style name="button_theme">
        <item name="android:textColor">@color/white</item>
        <item name="android:background">@drawable/custom_button_light_blue</item>
        <item name="android:textSize">30sp</item>
        <item name="textAllCaps">false</item>
    </style>
</resources>